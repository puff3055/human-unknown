# HUMAN UNKNOWN｜当前项目状态

> 文档版本：1.13
> 最后更新：2026-09-06
> 最新产品决策：`PD-017`
> 维护规则：每次产品范围、章节、实现基线或验收状态变化时，在同一次提交中更新本文档。

## 1. 当前基线

- 已实现候选范围：**V4.7 首页旁白／第一次接触 + 单一光点连续坠入 + 世界 1「THE GREEN BODY／群体生命」实时网络 + 首页／植物页共享交互声音**。
- 当前声音候选分支：`codex/interaction-sound-v1`；候选标签：`interaction-sound-v0.1.0-rc.1`。上一汇合回退点仍为 `codex/home-green-bridge`／`home-green-v0.1.0-rc.1`。
- 两个受保护的来源基线：V4.5.3 首页提交 `e894154`／标签 `v4.5.3-rc.1`；世界 1 实时网络提交 `968af59`。汇合没有改写这两个历史点。
- 世界 1 独立候选仍可从 `codex/green-body-inspiration-pages` 的 `968af59` 继续；本轮只把它接到最新首页，不替它扩写后续世界。
- `world1-v0.2.0-rc.1` 的六幕实现已保留为归档回退点，不再是当前页面入口。
- `world1-v0.1.0-rc.1` 保留为历史回退点，但其“翻页／PPT 感”、弱动效、交互不清与缩放发糊已被用户否定，不再是验收基线。
- 上一个首页直接回退点：`v4.5.3-rc.1`（首页与声音，不含世界 1）。
- 文档事实源分支：`codex/product-source-of-truth-v1`；文档回退标签：`product-source-v1.0.0` 起。
- GitHub 默认 `main` **未包含本轮汇合实现**；本轮不修改、合并或推送 `main`。验收候选只保存在独立汇合分支和标签中。
- 当前黑客松阶段的默认交付端是**桌面网页浏览器**。新世界优先完成鼠标驱动的单页核心互动；手机版、触摸与键盘等价路径、移动响应式、减少动态和低性能降级不在默认范围，只有用户重新明确要求时才加入。已有支持保留，不要求为此返工删除。

## 2. 首页现状

### 已实现

- 以 `首页素材1.png` 为视觉母版的星云／瞳孔／行星多义画面。
- 局部纹理凝视、好奇反应、鼠标穿水般的扰动与连续扩散。
- 生长、消亡和残余迁移共存的自主生命节律。
- 只在新鲜行为引起好奇时出现的一次性瞳孔变化。
- 可见的全局声音开关，声音主要与鼠标移动、靠近、停留、点击和传导回应绑定；无操作时回到安静。

### 已实现但待主观验收

- V3.4 声音的真实耳机／扬声器听感。

### 本轮新增且已验证

- 首页与植物页共用同一个 AudioContext、总音量上限和用户开关状态；两个页面都有同位置的可见音量键，任意一处开关都会同步。
- 植物页删除独立持续风噪／树叶感噪声、自动低频和进入脉冲；静止时声音平滑归零，只由鼠标移动、节点停留以及用户点击后真实传播到达触发。
- 桌面浏览器实验确认：植物页静止后 `presence=0`、`movement=0`；鼠标移动时声音能量出现，停止后自然衰减；音量键不会误触植物网络。真实耳机／扬声器音色仍待用户主观验收。
- 首页完整保留 V4.5.3 的片名、旁白、热区、三秒凝视、星云生命场、声音开关和纯光光标。
- 用户完成凝视后，同一个光点先锁定首页瞳孔，再沿 Z 轴坠入，经过一次硬切后收缩到植物网络的真实节点 `spore-center-low`；没有双光点、黑场按钮或首页／世界叠化。
- 世界 1 在光点抵达后才从 arrival 进入 local；章节自己的呼吸与网络回应不会在首页阶段提前运行。返回会重新载入首页，避免残留计时器、声场或传导状态。

## 3. 世界 1「THE GREEN BODY／群体生命」现状

### 当前已实现候选

- 首页吸入后进入一张全屏实时网络页，中标题 `CHAPTER 01`，大标题 `THE GREEN BODY`，中文章节名「群体生命」。
- 使用 `00-项目参考材料/01-世界1/世界1-更新2.png` 作为 WebGL 动态纹理母版，而非 CSS 静态背景。实现副本 `assets/world1-update-2-background.png` 与原图字节一致（1672×941），并提供 3344×1882 的本地 2× 采样纹理。底层 Shader 用手工空间遮罩把树膜、主干、根系、蘑菇与孢子分组重新取样，各组保持不同节律并能局部形变；不依赖外部收费抠图或生成服务。暗部遮罩覆盖母版烘焙的旧标题与底部提示。
- 鼠标移到任意位置时局部纹理被牵引并发亮。点击会先将感知接入最近组织，再进入一张按原图结构手工描出的隐藏传导图：51 个节点、64 条三次贝塞尔根／菌丝／树脉路径逐级分叉，包含前导光、信息主体、衰减尾迹、交汇停顿和约 1.75 秒的路径余辉；没有交互时网络轨迹完全不可见。
- 蘑菇、树膜、主干与孢子不再按预设时间假装响应。只有光真正抵达对应节点时才发出器官事件，触发该位置的局部亮度、位移与回弹；一次点击可穿过整张连通网络到达远端。
- 互动过程依次表达「这里感知到了你。」「那里，也是你。」，并收束到「如果整个生态网络才是意识的尺度呢？——《Avatar》」；章节标题在交互期间持续保留。
- 保留键盘点亮、减少动态、返回首页和无障碍状态播报。

### 浏览器验证已通过

- 1672×941 Chrome 中的章节转场、标题、悬停局部光、点击扩散与返回入口。
- 点击后整张隐藏网络完成远距离传导，自动验收确认抵达超过 55 个路径终点、触发超过 12 个真实器官回应，结束后在途信号与短时余辉归零；页面控制台零 warning/error。
- 汇合候选的桌面完整链路已通过：`opening → aligned → entering → handoff → arrival → local → whole`；硬切计数始终为 1，抵达节点正确，点击后 128 个路径抵达、46 次器官回应，完成态和返回首页均成立；控制台 warning/error 为 0。

### 历史回退候选

- 以 `00-项目参考材料/01-世界1/` 六张 1672 × 941 PNG 为严格顺序的视觉母版；实现副本哈希一致。
- 主命题固定为 `SELF / 个体边界`，次命题为 `OWNERSHIP / 资源归属`；用户是同一植物整体中暂时醒来的局部感知。
- 第二幕的生长、第三幕的回应、第四幕的下潜和第五幕的资源流动构成同一信号的连续状态；第五幕无输赢与道德正确答案。
- 第六幕只保留候选灵感来源、再次体验与返回入口，不提供伪下一章入口。
- 世界 1 使用独立控制器、样式和声音层，支持清晰进入、暂停、重玩和返回清理。
- v0.2 将六幕收进同一个原稿比例桌面舞台：稳定底图不裁切、不放大、不做合成缩放；同一光点通过推进、下潜与抵达切口持续出现，取消淡入淡出的幻灯片感。
- 第二、三、四、五幕具有可见的过程态、百分比与世界内反馈；桌面操作条明确说明当前该拖动、滚动还是点击。

### 自动验收已通过

- 1672 × 941 桌面完整路径，包括真实停留、场景推进、第二／三幕伸展与回应、第四幕下潜、第五幕停顿后延时流动。
- 完整键盘推进、`prefers-reduced-motion` 等价路径和静音路径。
- 六幕逐张原图／实现同屏设计 QA、重玩与返回清理、浏览器控制台零 warning/error、静态契约与 JavaScript 语法检查。

### 待用户主观验收／已知限制

- 共享交互声音已通过状态、音量平滑和页面控件验证；最终音色与舒适度仍需用户用真实耳机或扬声器试听。
- 本轮明确只交付桌面；移动端适配与移动端验收延后。
- 原始母版分辨率只有 1672 × 941。当前实现避免超过原图尺寸与稳定态缩放；若未来要求 4K 级别锐度，需要重新输出可靠的高分辨率母版，而不是在代码里伪造细节。
- 三项灵感来源仍是候选信息，尚未接未经核对的外部链接。

## 4. 独立互动候选：眼睛多重宇宙

- **已实现本地候选 v0.6／桌面浏览器自动验收通过／真实麦克风待用户复验：** `不同世界参考图03.png` 不作为整张背景；所有初始眼睛和新生眼睛都使用从原图中直接提取的同一透明眼球素材，在程序化星空、星云和连接网络中独立凝视、眨眼和分岔。用户每说一个 `hey` 新生成一个眼睛世界。
- 独立入口为 `02-代码仓库/eye-multiverse.html`；尚未并入首页旅程，也尚未确定它对应四章中的哪一章。
- 首屏不再放居中大段引导、紫色胶囊按钮或快捷键说明。底部只保留与首页同语汇的圆形麦克风、细字状态、实时声纹和短横线；入口默认可见，点击后依次明确显示等待权限、聆听、听到声音、识别、接收、未听清和失败状态。
- 语音识别启用连续聆听、中间结果和 10 个候选结果，并兼容 `hey/heyy/hei/hai/hay/hi/嗨/嘿/黑/海` 等常见识别结果。真实麦克风、不同口音和现场噪声仍须由用户在设备上复验；自动验收没有代替用户接受麦克风权限。
- 正式动画素材 `assets/eye-orb-source-cutout.png` 为 436×436 真实 Alpha PNG；眼球内部像素直接来自原图，未使用重绘结果。WebGL 运行时以亮度透射消解素材中的黑色底片，并重建背景折射、球壳边缘、高光和内部瞳孔，因此眼睛不再呈现方形或圆盘式抠图边界。
- v0.6 初始只保留 3 颗原生眼。外层球壳、高光与内部虹膜分层绘制：凝视只驱动虹膜和瞳孔，外壳反光保持在世界光源上；每颗眼睛会独立选择空间或其他眼睛作为注意目标，听见后才以不同延迟看向事件。
- 一次回应不再拆成多段命名流程，而是“声音信号命中母眼 → 完整球体从共享表面剥离 → 细桥稳定”的连续物理动作。回执立即出现，约 0.9–1.0 秒计数，约 1.25 秒完全稳定；后续 `hey` 每 380ms 可发起下一条并行分支，不再串行等待上一颗结束。
- 眨眼改为球壳内部的非对称瞬膜：上膜承担主要闭合、下膜只轻微抬起，闭合快于打开，含短暂闭合停留、横向滑动和少量不完全眨眼。膜扩展到球体内部而非虹膜中央圆区；环境光和外壳裂纹在眨眼期间保持可见。
- 世界计数持续增长，不设失败终点或“现实崩塌”。到 8／21／55 个新现实后，视野逐级后退、连接网络变亮、星空密度增强，表达结构相变；实时前景保留主眼与最新一组分支以控制 GPU 成本，较早分支退出近景后由整体星空承担“远去宇宙”的空间语义。
- 当前范围明确只交付桌面网页端；手机版、触摸布局和移动端验收不在本轮范围。

## 5. 当前产品旅程

| 部分 | 状态 | 已确定 | 仍可探索 |
| --- | --- | --- | --- |
| 序章：第一次接触 | V4.5.3 已实现，并已接入第一章候选 | 星云瞳孔、互相注意、中央停留后吸入；同一光点完成跨世界连续性 | 转场视觉节奏与声音主观混音待用户验收 |
| 第一章：植物／群体智能 | 单页实时网络候选已接入／待用户主观验收 | 局部感知通过真实隐藏网络抵达远端器官，逐步显出“整体生命” | 章节交互与声音主观微调；移动端另行排期 |
| 第二章：硅基／非生物智能 | 当前方向 | 探索不依赖熟悉生物身体的智能 | 具体载体、文明形态、玩法及其与前后章的关系 |
| 第三章：高维／时空智能 | 当前方向 | 探索超出人类熟悉空间、时间与现实经验的存在 | 它如何被感知、如何生活、如何与用户接触 |
| 第四章：The Unnamed | 名称已确定／体验待设计 | `The Unnamed` 作为继续超出既有命名的方向 | 故事、玩法、视听、如何避免只剩黑屏与宏大旁白 |

四章是当前的整体产品架构，不等于其中的故事、玩法和实现已经冻结。每个 Agent 只需围绕当前明确的部分展开。

## 6. 不是当前方案的内容

以下都只能作为历史素材或灵感，不得自动恢复成任务：

- 把“极速文明”定为第一章或整体主线。
- 固定的 8 个世界或 Layer 0–6 七层路线。
- 已暂停的“截面之外 → 交换场 → 仍在发生”原型。
- 把每个世界强制收束成相同的心理启示或“反观人类”模板。
- 把项目做成科幻资料展、普通电影页或只能观看的动画。

## 7. 本文档何时必须更新

- 实现版本、分支、标签或验收状态变化。
- 章节数量、顺序、名称或开发优先级变化。
- 某个“当前方案”被确认、否定或实现。
- 新 Agent 应从不同基线开始。

更新本文档时，同时在 [产品决策与更新记录](01-AI做的文档/13-产品决策与更新记录.md) 追加一条记录。
