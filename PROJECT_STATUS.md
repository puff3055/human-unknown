# HUMAN UNKNOWN｜当前项目状态

> 文档版本：1.4
> 最后更新：2026-09-06
> 最新产品决策：`PD-008`
> 维护规则：每次产品范围、章节、实现基线或验收状态变化时，在同一次提交中更新本文档。

## 1. 当前基线

- 已实现范围：**只有首页／第一次接触及其黑场出口**；没有实现植物世界。
- 当前首页分支：`codex/light-cursor-v4-3`。
- 当前候选标签：`v4.3.0-rc.1`。
- V4.2 光标替换前的直接回退点：`v4.2.0-rc.1`（核心实现 `89e98ab`）。
- V4.1 直接回退点：`v4.1.0-rc.1`（`51e9337`）。
- V4.0 直接回退点：`v4.0.0-rc.1`（`f71c83f`）。
- 仍可直接回退的 V3.4 基线：`v3.4.0-rc.1`（`a79fe83`），标签保留不变。
- V4.3 在 V4.2 完整实现上只替换自定义光标，不重写其他首页系统。

## 2. 首页现状

### 已实现

- 以 `首页素材1.png` 为视觉母版的星云／瞳孔／行星多义画面。
- 局部纹理凝视、好奇反应、鼠标穿水般的扰动与连续扩散。
- 生长、消亡和残余迁移共存的自主生命节律。
- 电影式短黑场、约 3.6 秒黑位显影与片名入场；标题完整可读至少 4 秒，由第一次有意识移动解锁后续接触。片名和固定副标题入场后保持常驻，不在接触开始时消失。
- 旁白固定在片名上方，只保留“在这里” → “它注意到你了” → “放入你的眼睛”三句；“对，在这里”已删除。完整可读时间约为 1.6／2.0／1.8 秒，替换动作也缩短。
- 外围／核心热区使用不对称暗银有机光提示。最后一句完整显示并溶出前，核心停留始终不计时；之后瞳孔收窄变形，显示 3/2/1，连续凝视满 3 秒才进入，离开柔和回落。
- 坠入在约 0.42 秒静止后，由单一 Z 轴缩放从 `1.05` 单调增至 `4.60`，不再出现先缩小再放大；约 4.2 秒后交付黑场。
- 桌面、触摸／笔和键盘 Enter／Space 等价输入；`prefers-reduced-motion` 保留阅读与行为门，停止 WebGL 动画和自主代谢。
- 声场以 35–55 Hz 质量和 80–120 Hz 窄带泛音为核心；新会话默认为“希望有声”的 `armed` 状态，明示“轻触开启声音”，一次合法手势即启声并播放一次低频唤醒回应。用户明确关闭会持久保留。
- 声音增加约 44 Hz／99 Hz 的微弱有机漂移底层，稀疏压力间隔缩短为约 5–9 秒；仍无白噪、air hiss 或压缩器。
- 自定义光标使用用户选定参考光的透明素材：默认是暖白／暗银游丝光团，进入声音按钮或中央外围／核心热区时收束为高亮光点。
- 光芯精确跟手，外围游丝响应速度并保留少量惯性；按下时轻微压缩，坠入时朝锁定瞳孔拉伸、收缩、溶解并在黑场前完全被吸收。
- 光标在 `prefers-reduced-motion` 下取消游丝旋转与速度拖曳，触摸／粗指针设备使用原生光标并隐藏自定义光。
- 稳定集成契约：`data-intro`、`data-phase`、`data-hotzone`、`data-hold`、`data-entry`、`data-countdown`、`data-countdown-value`、`data-exit-ready`、光标的 `data-mode`／`data-target`、`--contact-countdown`、`--hotzone-cue`／`--hotzone-scale` 等 CSS 变量与 `humanunknown:homepage-exit` 事件。

### 客观测试通过

- `qa/homepage-v4-contract-test.mjs` 通过：三句精确文案、常驻片名、旁白后硬门、3/2/1 三秒凝视、停留／回落、触摸抬起取消、单调坠入、黑场出口与 reduced motion。
- `qa/soundscape-contract-test.mjs` 通过：默认 `armed`、一次手势启声、唤醒回应、可感窄带底层、noticed 让位和旧噪声层禁回归。
- `qa/light-cursor-contract-test.mjs` 通过：默认光团、中央／声音交互光点、按压、速度惯性、被吸收、透明素材与 reduced-motion 契约。
- 内置浏览器已检查桌面 1440×900、移动 390×844 布局、reduced-motion 与完整接触链；实测光标为 `cloud → point → absorbing`，黑场前透明度为 0，控制台 warning/error 均为 0。当前光标证据在 `02-代码仓库/qa/v4.3/`；V4.2 页面流程证据仍在 `02-代码仓库/qa/v4.2/`。

### 已实现但待用户主观验收

- V4.3 纯光光标的真实页面视觉：光团大小、亮度、游丝惯性、光点精确感和被吸收的速度是否符合用户心中的参考感。
- 沿用的 V4.2 真实耳机／普通扬声器听感：启声回应是否清晰但不突兀、有机底层是否足够可感又不变成机器持续运转、低频重量、普通设备泛音、移动水感长尾、一次性注意张开、边界近静音和下弯坠落。

### 本轮明确未实现

- 黑场之后的植物／群体智能世界。

## 3. 当前产品旅程

| 部分 | 状态 | 已确定 | 仍可探索 |
| --- | --- | --- | --- |
| 序章：第一次接触 | V4.3 已实现候选／光标待视觉验收／声音待试听 | 电影式开场、常驻片名、三句接触、三秒凝视、纯光双形态光标、单向坠入、黑场出口 | 光标主观微调，真实设备声音混音微调 |
| 第一章：植物／群体智能 | 已确定章节与核心感受／故事待设计 | 植物不是死寂背景；它们拥有丰富感知与回应，并与更大整体相连 | 具体故事、世界规则、交互、声音、章尾推荐 |
| 第二章：硅基／非生物智能 | 当前方向 | 探索不依赖熟悉生物身体的智能 | 具体载体、文明形态、玩法及其与前后章的关系 |
| 第三章：高维／时空智能 | 当前方向 | 探索超出人类熟悉空间、时间与现实经验的存在 | 它如何被感知、如何生活、如何与用户接触 |
| 第四章：The Unnamed | 名称已确定／体验待设计 | `The Unnamed` 作为继续超出既有命名的方向 | 故事、玩法、视听、如何避免只剩黑屏与宏大旁白 |

四章是当前的整体产品架构，不等于其中的故事、玩法和实现已经冻结。每个 Agent 只需围绕当前明确的部分展开。

## 4. 不是当前方案的内容

以下都只能作为历史素材或灵感，不得自动恢复成任务：

- 把“极速文明”定为第一章或整体主线。
- 固定的 8 个世界或 Layer 0–6 七层路线。
- 已暂停的“截面之外 → 交换场 → 仍在发生”原型。
- 把每个世界强制收束成相同的心理启示或“反观人类”模板。
- 把项目做成科幻资料展、普通电影页或只能观看的动画。

## 5. 本文档何时必须更新

- 实现版本、分支、标签或验收状态变化。
- 章节数量、顺序、名称或开发优先级变化。
- 某个“当前方案”被确认、否定或实现。
- 新 Agent 应从不同基线开始。

更新本文档时，同时在 [产品决策与更新记录](01-AI做的文档/13-产品决策与更新记录.md) 追加一条记录。
