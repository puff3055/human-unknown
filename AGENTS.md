# HUMAN UNKNOWN｜Agent 协作与文档维护规则

这份文件管理“Agent 怎样获得最新上下文”，不规定每个世界必须长什么样。产品说明保护的是项目的原点，而不是一套不能变的细节答案。

## 1. 每次任务的必读顺序

1. 完整阅读根目录 [README.md](README.md)。
2. 完整阅读 [12-HUMAN_UNKNOWN_PRODUCT_BRIEF.md](01-AI做的文档/12-HUMAN_UNKNOWN_PRODUCT_BRIEF.md)。
3. 阅读 [PROJECT_STATUS.md](PROJECT_STATUS.md)，确认当前实现、候选方案、分支、版本和待验收项。
4. 查看 [13-产品决策与更新记录.md](01-AI做的文档/13-产品决策与更新记录.md) 的最新记录。
5. 准备修改时阅读 [04-版本管理与改动确认规则.md](01-AI做的文档/04-版本管理与改动确认规则.md)。
6. 只再阅读与当前任务有关的规格、代码和参考材料。
7. 动手前检查 `git status`、当前分支、最新提交与并行修改，不覆盖其他人的工作。

不要通过编号、文件名、日期或“看起来最完整”自行判定哪份是当前方案。
原始完整对话用于追溯前因后果，不是默认必读的当前需求；只有任务确实需要历史语境时才读，并用现行事实源重新核对其中结论。

## 2. 信息优先级

出现冲突时，按以下顺序判断：

1. 用户在当前任务中明确、完整表达的最新要求。
2. 产品说明中的“产品原点”与“已确认”内容。
3. `PROJECT_STATUS.md` 中的当前状态。
4. 已经用户确认的当前任务规格。
5. 产品决策与更新记录。
6. 原始对话、旧方案和历史代码，它们只能解释过程，不能覆盖现行结论。

中断的任务、尚未完成的回答、草稿或用户已明确停止的方向，不进入产品事实源。

## 3. 统一状态标识

- **产品原点**：为什么做、为谁做、希望人经历什么。产品功能变化时默认不变；只能由用户的明确产品决策修改。
- **已确认**：用户已明确选定的当前决定。它可以在以后修改，但修改必须留记录。
- **已实现**：代码中已存在；不自动等于用户已验收。
- **当前方案**：正在探索或推荐的可行路线，可以通过讨论、研究和原型改变。
- **待探索**：只有问题或方向，还没有方案。
- **历史归档**：已被取代、否定、暂停或只供追溯的内容。除非用户明确重启，不得作为当前实现依据。

Agent 在提案时应明确说出引用的内容属于哪种状态，不要把“当前方案”说成“已确认”。

## 4. 创作自由

- 产品说明定义目标体验，不默认规定唯一的美术、镜头、台词、玩法或技术栈。
- Agent 可以主动提出新的世界规则、交互、声音和故事，但要说明它如何服务这次相遇。
- 不要因为旧文档更详细，就把它的参数、世界或结尾复制到新方案。
- 只有会改变产品原点、章节核心、整体顺序或已确认方向的选择，需要先获得用户确认。任务范围内的细节可以主动探索并制作可验证原型。

## 5. 产品变更后必须同步的文档

一次产品变更只有完成下列动作后才算交付：

1. 在 [13-产品决策与更新记录.md](01-AI做的文档/13-产品决策与更新记录.md) 追加新记录，不改写旧记录。
2. 如果当前范围、优先级、版本或验收状态改变，更新 `PROJECT_STATUS.md`。
3. 只有产品原点或已确认的整体架构改变时，才修改产品说明。不要把每个参数和小功能塞进产品说明。
4. 如果旧文档已被替代，将它移入归档目录、增加归档警示，并修正所有现行链接。
5. 文档与相关代码尽量在同一分支和同一次提交中更新，并保留标签或可识别的回退点。

每条更新记录至少包含：日期、状态、决定、代替的旧内容、影响范围、依据和对应分支／版本。

交付前做一次时效核对：

- `PROJECT_STATUS.md` 顶部的“最新产品决策”应与更新记录最上方的决策 ID 一致；
- 项目状态里声明的分支、提交、标签和实际 Git 状态应能对应；
- 现行入口中的相对链接应有效，历史文件开头应保留归档警示；
- 如果本轮改变了产品或实现状态却没有触发任何文档更新，要明确说明为什么，而不是默认漏记。

## 6. 工作方式

- 用户要求先出方案时，未确认前不修改代码。
- 改动前先保存可回退点；一个分支只解决一个明确目标。
- 保留用户的现有改动和并行 Agent 的文件所有权，不回退无关工作。
- 修改后根据风险做真实验收；无法验收时明确记录缺口，不把“已实现”写成“已验收”。
- 新 Agent 在开工回执中用简短段落说明：本次任务服务的产品原点、引用的已确认内容、仍可自由设计的部分。
